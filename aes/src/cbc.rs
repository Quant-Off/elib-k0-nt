use crate::AES256;
use zeroize::Zeroize;

pub const CBC_IV_SIZE: usize = 16;

pub struct AES256CBC {
    cipher: AES256,
}

impl AES256CBC {
    #[must_use]
    pub fn new(key: &[u8; 32]) -> Self {
        Self {
            cipher: AES256::new(key),
        }
    }

    pub fn encrypt(&self, iv: &[u8; CBC_IV_SIZE], plaintext: &[u8], ciphertext: &mut [u8]) {
        assert!(
            plaintext.len().is_multiple_of(16),
            "평문 길이가 블록 크기의 배수가 아님"
        );
        assert!(
            ciphertext.len() >= plaintext.len(),
            "암호문 버퍼가 평문보다 작아 무음 절단 발생"
        );

        let mut prev = *iv;

        for (pt_chunk, ct_chunk) in plaintext.chunks(16).zip(ciphertext.chunks_mut(16)) {
            let mut block = [0u8; 16];
            block.copy_from_slice(pt_chunk);

            for i in 0..16 {
                block[i] ^= prev[i];
            }

            let encrypted = self.cipher.encrypt(&block);
            ct_chunk.copy_from_slice(&encrypted);
            prev = encrypted;
            block.zeroize();
        }
    }

    pub fn decrypt(&self, iv: &[u8; CBC_IV_SIZE], ciphertext: &[u8], plaintext: &mut [u8]) {
        assert!(
            ciphertext.len().is_multiple_of(16),
            "암호문 길이가 블록 크기의 배수가 아님"
        );
        assert!(
            plaintext.len() >= ciphertext.len(),
            "평문 버퍼가 암호문보다 작아 무음 절단 발생"
        );

        let mut prev = *iv;

        for (ct_chunk, pt_chunk) in ciphertext.chunks(16).zip(plaintext.chunks_mut(16)) {
            let mut block = [0u8; 16];
            block.copy_from_slice(ct_chunk);

            let mut decrypted = self.cipher.decrypt(&block);

            for i in 0..16 {
                pt_chunk[i] = decrypted[i] ^ prev[i];
            }

            prev.copy_from_slice(ct_chunk);
            decrypted.zeroize();
            block.zeroize();
        }
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn cbc_nist_f_2_5() {
        let key: [u8; 32] = [
            0x60, 0x3d, 0xeb, 0x10, 0x15, 0xca, 0x71, 0xbe, 0x2b, 0x73, 0xae, 0xf0, 0x85, 0x7d,
            0x77, 0x81, 0x1f, 0x35, 0x2c, 0x07, 0x3b, 0x61, 0x08, 0xd7, 0x2d, 0x98, 0x10, 0xa3,
            0x09, 0x14, 0xdf, 0xf4,
        ];
        let iv: [u8; 16] = [
            0x00, 0x01, 0x02, 0x03, 0x04, 0x05, 0x06, 0x07, 0x08, 0x09, 0x0a, 0x0b, 0x0c, 0x0d,
            0x0e, 0x0f,
        ];
        let plaintext: [u8; 64] = [
            0x6b, 0xc1, 0xbe, 0xe2, 0x2e, 0x40, 0x9f, 0x96, 0xe9, 0x3d, 0x7e, 0x11, 0x73, 0x93,
            0x17, 0x2a, 0xae, 0x2d, 0x8a, 0x57, 0x1e, 0x03, 0xac, 0x9c, 0x9e, 0xb7, 0x6f, 0xac,
            0x45, 0xaf, 0x8e, 0x51, 0x30, 0xc8, 0x1c, 0x46, 0xa3, 0x5c, 0xe4, 0x11, 0xe5, 0xfb,
            0xc1, 0x19, 0x1a, 0x0a, 0x52, 0xef, 0xf6, 0x9f, 0x24, 0x45, 0xdf, 0x4f, 0x9b, 0x17,
            0xad, 0x2b, 0x41, 0x7b, 0xe6, 0x6c, 0x37, 0x10,
        ];
        let expected_ciphertext: [u8; 64] = [
            0xf5, 0x8c, 0x4c, 0x04, 0xd6, 0xe5, 0xf1, 0xba, 0x77, 0x9e, 0xab, 0xfb, 0x5f, 0x7b,
            0xfb, 0xd6, 0x9c, 0xfc, 0x4e, 0x96, 0x7e, 0xdb, 0x80, 0x8d, 0x67, 0x9f, 0x77, 0x7b,
            0xc6, 0x70, 0x2c, 0x7d, 0x39, 0xf2, 0x33, 0x69, 0xa9, 0xd9, 0xba, 0xcf, 0xa5, 0x30,
            0xe2, 0x63, 0x04, 0x23, 0x14, 0x61, 0xb2, 0xeb, 0x05, 0xe2, 0xc3, 0x9b, 0xe9, 0xfc,
            0xda, 0x6c, 0x19, 0x07, 0x8c, 0x6a, 0x9d, 0x1b,
        ];

        let cbc = AES256CBC::new(&key);
        let mut ciphertext = [0u8; 64];
        cbc.encrypt(&iv, &plaintext, &mut ciphertext);
        assert_eq!(ciphertext, expected_ciphertext);

        let mut decrypted = [0u8; 64];
        cbc.decrypt(&iv, &ciphertext, &mut decrypted);
        assert_eq!(decrypted, plaintext);
    }

    #[test]
    fn cbc_roundtrip() {
        let key: [u8; 32] = [
            0xde, 0xad, 0xbe, 0xef, 0xca, 0xfe, 0xba, 0xbe, 0x01, 0x23, 0x45, 0x67, 0x89, 0xab,
            0xcd, 0xef, 0xfe, 0xdc, 0xba, 0x98, 0x76, 0x54, 0x32, 0x10, 0x00, 0x11, 0x22, 0x33,
            0x44, 0x55, 0x66, 0x77,
        ];
        let iv: [u8; 16] = [
            0x01, 0x02, 0x03, 0x04, 0x05, 0x06, 0x07, 0x08, 0x09, 0x0a, 0x0b, 0x0c, 0x0d, 0x0e,
            0x0f, 0x10,
        ];
        let plaintext: [u8; 32] = [
            0x48, 0x65, 0x6c, 0x6c, 0x6f, 0x2c, 0x20, 0x57, 0x6f, 0x72, 0x6c, 0x64, 0x21, 0x00,
            0x00, 0x00, 0x54, 0x68, 0x69, 0x73, 0x20, 0x69, 0x73, 0x20, 0x61, 0x20, 0x74, 0x65,
            0x73, 0x74, 0x21, 0x00,
        ];

        let cbc = AES256CBC::new(&key);
        let mut ciphertext = [0u8; 32];
        cbc.encrypt(&iv, &plaintext, &mut ciphertext);

        let mut decrypted = [0u8; 32];
        cbc.decrypt(&iv, &ciphertext, &mut decrypted);
        assert_eq!(decrypted, plaintext);
    }
}
